{
  "name": "community-network-management",
  "version": "1.0.0",
  "description": "社区网络设备维修管理系统",
  "private": true,
  "scripts": {
    "prestart": "cd frontend && npm ci && npm run build",
    "start": "node backend/server.js",
    "build": "cd frontend && npm ci && npm run build && cd ../backend && npm ci"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^7.6.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^8.5.1",
    "multer": "^1.4.5-lts.1"
  },
  "engines": {
    "node": ">=14.18.0"
  }
}
